First install requirements.txt using command 
pip install -r requirements.txt


Download this repository open My Project and  extract model.rar in same directory.
it create folder named as model it contains "model.pickle" 
Now open terminal in same direcotry ( My Project )
Then type command:-
python Risk.py or py Risk.py
Now visit localhost  http://127.0.0.1:5000 (ensure port Number)
